{
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check relay-hub.js && node --check relay-agent.js && node --check scripts/test-relay.js",
    "relay:hub": "node relay-hub.js",
    "relay:agent": "node relay-agent.js",
    "relay:test": "node scripts/test-relay.js"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  },
  "private": true,
  "name": "codex-mini",
  "version": "3.0.5",
  "description": "Local phone-to-Codex/Mac typing bridge",
  "license": "SEE LICENSE IN LICENSE"
}
